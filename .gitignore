# Downloaded / patched OpenAPI specs (fetched fresh at publish time)
spec.json
spec.patched.json

# SDK build & generator artifacts
dist/
build/
*.tgz
*.egg-info/
node_modules/
.openapi-generator/
.openapi-generator-cli/
openapitools.json

# Python
__pycache__/
*.pyc

# local secrets (YakYak PAT for scripts / regen tooling)
e2e/.env.bb
e2e/.env*
*.env.bb

# course / local SDK builds
course/.env
course/node_modules/
sdk/javascript/
sdk/python/
.venv/
.DS_Store
course/package-lock.json

# local parking lot for files pending deletion (never commit)
integrations/potentially_unused/
